# Power-Automate-Project
Power Automate Desktop project for processing and validating invoice batch files with PowerShell-based data validation, analysis, and reporting. The workflow classifies valid and invalid inputs, logs errors and unsupported file types, generates summary outputs, and archives results in run-based folders.
